export default defineEventHandler(async () => {
  // type News = {
  //   title?: string;
  //   content?: string;
  // };
  // const { data } = await useMicroCMSGetList<News>({
  //   endpoint: "news",
  //   queries: { limit: 10 },
  // });
  // return data;
});
